const express = require('Express')
const app = express()
const mongoose =require('mongoose')
const User=require('../models/Users');


const dbURI='mongodb+srv://PoseFit:PoseFit@cluster.y1yvcw2.mongodb.net/PoseFit?retryWrites=true&w=majority';
mongoose.connect(dbURI,{useNewUrlParser:true , useUnifiedTopology:true})
.catch((err)=>console.log(err));

const login= (req, res) => {
    User.exists({name:req.body.email , password:req.body.password})
    .then((result)=>{
        res.send({status:true});
        console.log(result);
    })
    .catch((err)=>{
        console.log(err);
    });

   /* const user=new User({
        email: req.body.email,

        password:req.body.password
    });
    user.save()
    .then((result)=>{
        res.send('student added');
    })
    .catch((err)=>{
        console.log(err);

    });*/
    }
    module.exports={
        login
    }